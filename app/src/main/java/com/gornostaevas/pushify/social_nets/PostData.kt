package com.gornostaevas.pushify.social_nets

/**
 * Holds data necessary for a single post
 * Can hold a path to a picture (or something like that in the furure, so its expandable
 */
data class PostData(val title: String, val content : String) {
}